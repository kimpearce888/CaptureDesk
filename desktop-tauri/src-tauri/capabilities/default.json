{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "CaptureDesk application windows: core APIs plus the dialog plugin. Window show/hide/focus/close are granted so the region picker can reveal itself after the screenshot decodes and close itself as an escape hatch.",
  "windows": ["main", "toolbar", "region", "cambubble", "editor"],
  "permissions": [
    "core:default",
    "dialog:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:window:allow-close"
  ]
}
